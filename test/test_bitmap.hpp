#pragma once

template<typename _Tp>
bool test_bitmap_(_Tp *data_seed, long long n){
    srand(time(NULL));
    const int N = 100;
    unsigned long long* x[N];
    for (int i = 0; i < N; ++i) x = rand();
}